
[comment]: <> (THIS FILE IS GENERATED AS LONG AS THIS LINE EXISTS)

# Stereotype "JavaLinApp"

This Stereotype marks a class as the main-class for a JavaLin application. Each method specified in that class can be marked with a RequestHandler stereotype in order to full fill incoming requests.

JavaLin in this cartridge is intended to implement small but time consuming scalable business cases that can be executed parallel.

Each JavaLin app will create a complete project module with its own
build file and docker image. The foot print of JavaLin is very small so you should be able to start many replicas of such an docker container.


## BaseClass(es)
This stereotype is applicable to the following UML-ELements:

* Element


## Associated Tagged Values
This Stereotype has no associated tagged vales.
