  # {{moduleName}} is a JavaLin driven module
  {{moduleName}}:
    container_name: "{{containerName}}"
    build:
      context: ./{{moduleName}}
      dockerfile: Dockerfile
    restart: always
    ports:
       - "{{modulePort}}:{{applPort}}"
